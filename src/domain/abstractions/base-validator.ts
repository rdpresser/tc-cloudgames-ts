// //import { AbstractValidator } from "fluent-ts-validator";

// export abstract class BaseValidator_OLD<T> extends AbstractValidator<T> {
//   /**
//    * Validates the entity and returns a ValidationResult.
//    * @param entity The entity to validate.
//    * @returns A ValidationResult containing validation failures, if any.
//    */
//   // public validateResult(entity: T) : ValidationResult {

//   //   return this.validate(entity);
//   // }
// }

// // export interface Validator<T> {
// //   validateResult(entity: T): ValidationResult;
// // }
